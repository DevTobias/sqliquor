{
  "name": "backend",
  "private": true,
  "license": "MIT",
  "workspaces": {
    "packages": [
      "modules/*",
      "app"
    ]
  },
  "scripts": {
    "turbo": "turbo run --no-daemon"
  },
  "devDependencies": {
    "turbo": "^1.10.3"
  }
}
