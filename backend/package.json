{
  "name": "backend",
  "private": true,
  "license": "MIT",
  "workspaces": {
    "packages": [
      "modules/*",
      "app"
    ]
  },
  "scripts": {
    "turbo": "turbo --no-daemon run"
  },
  "devDependencies": {
    "turbo": "^1.10.3"
  }
}
